{
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js",
    "dev": "concurrently \"cd server && nodemon index.js\" \"cd client && npm run dev\""
  }
}

